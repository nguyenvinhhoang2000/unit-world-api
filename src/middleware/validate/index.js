const UserSchema = require('./user.schema')
const ProjectSchema = require('./project.schema')
const RankSchema = require('./rank.schema')

module.exports = {
    UserSchema,
    ProjectSchema,
    RankSchema
}
